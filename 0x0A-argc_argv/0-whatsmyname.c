#include <stdio.h>

/**
 * main - Entry point.
 * Description: a program that prints its name, followed by a new line.
 * @argc: number of arguments passed on CLI.
 *@argv: Array of arguments passed on CLI.
 * Return: Always 0(success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
